// "use strict";

// function receiveUser(user) {
//   return user !== null ? user.username : "no user found";
// }

// console.log(receiveUser(null));

// a || b || c || d    повертає пертого thuthy або останній
// a && b && c && d    повертає першого falthy або останній
// a ?? b ?? c ?? d    повертає першого не-null або не-undefined або останній
